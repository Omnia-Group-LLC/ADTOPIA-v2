
describe('TenantContext', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

      </TenantProvider>
    );

    await waitFor(() => {
    });
  });

  it('should update document title with tenant name', async () => {
      </TenantProvider>
    );

    await waitFor(() => {
      expect(document.title).toBe('BixBox - Ad Card Platform');
    });
  });
